const server = require("../api/server");
const supertest = require("supertest");

// Write your tests here
test("sanity", () => {
	expect(true).toBe(true);
});

//api/auth/register

describe("POST: Able to register and authenticate new users", () => {
	test("return list of users", async () => {});
	test("return list of users", async () => {});
});

//api/auth/login

describe("POST: Able to login and verify user with token", () => {
	test("return list of users", async () => {});
	test("return list of users", async () => {});
});

//api/auth/users

describe("GET: Able to hit protected endpoint", () => {
	test("return list of users", async () => {});
	test("return list of users", async () => {});
});
